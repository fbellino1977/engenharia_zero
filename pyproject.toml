[project]
name = "engenharia-zero"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "email-validator>=2.3.0",
    "pydantic>=2.13.0",
    "pytest>=9.0.3",
    "ruff>=0.15.10",
]

[tool.setuptools]
packages = ["engenharia_zero"]

[tool.pytest.ini_options]
pythonpath = ["."]
